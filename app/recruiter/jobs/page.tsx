"use client";import {useEffect,useState} from "react";import Link from "next/link";import {ArrowLeft,ArrowRight,BriefcaseBusiness,Plus} from "lucide-react";type Job={id:string;title:string;city?:string;status?:string;employment_type?:string;created_at?:string};export default function RecruiterJobs(){const[jobs,setJobs]=useState<Job[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");useEffect(()=>{fetch("/api/recruiter/jobs",{cache:"no-store"}).then(async r=>{const d=await r.json();if(r.status===401){location.href="/recruiter/login";return}if(!r.ok)throw new Error(d.error||"Unable to load jobs");setJobs(d.jobs||[])}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <main className="horizon-page bg-slate-50"><div className="horizon-container py-9"><div className="flex flex-wrap items-center justify-between gap-4"><Link href="/recruiter/dashboard" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft size={15}/> Dashboard</Link><Link href="/recruiter/post-job" className="horizon-button horizon-button-gold"><Plus size={16}/> Post a job</Link></div><div className="mt-6"><p className="horizon-eyebrow">Recruiter workspace</p><h1 className="mt-2 text-3xl font-black">Manage jobs</h1><p className="mt-2 text-sm text-slate-500">Only listings belonging to your company workspace are shown.</p></div>{loading?<div className="horizon-card mt-7 p-10 text-center">Loading your jobs…</div>:error?<div className="mt-7 rounded-2xl border border-red-200 bg-red-50 p-5 text-sm text-red-700">{error}</div>:jobs.length?<div className="mt-7 grid gap-4">{jobs.map(j=><Link key={j.id} href={`/recruiter/jobs/${j.id}`} className="horizon-card horizon-card-hover flex flex-col gap-5 p-6 sm:flex-row sm:items-center sm:justify-between"><div className="flex gap-4"><span className="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl bg-[#071a35] text-[#e4ad2f]"><BriefcaseBusiness/></span><div><h2 className="font-black text-[#071a35]">{j.title}</h2><p className="mt-1 text-sm text-slate-500">{[j.city,j.employment_type?.replaceAll("_"," ")].filter(Boolean).join(" · ")||"Details not specified"}</p></div></div><div className="flex items-center gap-3"><span className="rounded-full bg-amber-50 px-3 py-1 text-[10px] font-black uppercase text-amber-700">{j.status||"draft"}</span><ArrowRight size={17} className="text-slate-300"/></div></Link>)}</div>:<div className="horizon-card mt-7 p-12 text-center"><BriefcaseBusiness className="mx-auto text-slate-300" size={40}/><h2 className="mt-4 font-black">No jobs yet</h2><p className="mt-2 text-sm text-slate-500">Create a listing and it will appear here after submission.</p><Link href="/recruiter/post-job" className="horizon-button horizon-button-gold mt-5">Post first job</Link></div>}</div></main>}
