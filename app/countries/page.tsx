import Link from "next/link";
import { ArrowRight, Globe2 } from "lucide-react";
import { createClient } from "@/lib/supabase-server";
export default async function CountriesPage(){const supabase=await createClient();const {data,error}=await supabase.from("countries").select("id,name,code").order("name");const countries=data??[];return <main className="horizon-page"><section className="bg-[#071a35] py-14 text-white"><div className="horizon-container"><p className="horizon-eyebrow">Global opportunities</p><h1 className="mt-3 text-4xl font-black md:text-5xl">Explore by country</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-white/60">Discover opportunities from the live country catalog.</p></div></section><section className="horizon-container horizon-section">{error?<div className="horizon-card border-red-200 bg-red-50 p-7 text-sm text-red-700">Unable to load countries. Check the Supabase connection.</div>:countries.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{countries.map(c=><Link key={c.id} href={`/countries/${c.code.toLowerCase()}`} className="horizon-card horizon-card-hover group flex items-center justify-between p-6"><div className="flex items-center gap-4"><span className={`fi fi-${c.code.toLowerCase()} h-7 w-9 rounded-sm`} /><div><h2 className="font-black text-[#071a35]">{c.name}</h2><p className="mt-1 text-xs text-slate-500">Browse jobs in {c.name}</p></div></div><ArrowRight size={17} className="text-slate-300 group-hover:text-[#b88410]"/></Link>)}</div>:<div className="horizon-card p-12 text-center"><Globe2 className="mx-auto text-slate-300" size={40}/><h2 className="mt-4 font-black">No countries available</h2><p className="mt-2 text-sm text-slate-500">No country records are currently available.</p></div>}</section></main>}
