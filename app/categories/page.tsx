import Link from "next/link";
import { ArrowRight, BriefcaseBusiness } from "lucide-react";
import { createClient } from "@/lib/supabase-server";

export default async function CategoriesPage(){ const supabase=await createClient(); const {data,error}=await supabase.from("categories").select("id,name,slug,icon").order("name"); const categories=data??[]; return <main className="horizon-page"><section className="bg-[#071a35] py-14 text-white"><div className="horizon-container"><p className="horizon-eyebrow">Explore opportunities</p><h1 className="mt-3 text-4xl font-black md:text-5xl">Jobs by category</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-white/60">Browse professional fields using the live category catalog.</p></div></section><section className="horizon-container horizon-section">{error?<ErrorBox/>:categories.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{categories.map(c=><Link key={c.id} href={`/categories/${c.slug}`} className="horizon-card horizon-card-hover group p-6"><div className="flex h-12 w-12 items-center justify-center rounded-xl bg-[#071a35] text-lg font-black text-[#e4ad2f]">{c.icon||c.name.charAt(0)}</div><h2 className="mt-5 font-black text-[#071a35]">{c.name}</h2><p className="mt-2 text-sm text-slate-500">Browse {c.name} opportunities</p><div className="mt-5 flex items-center gap-1 text-xs font-black text-[#b88410]">Explore <ArrowRight size={14} className="transition group-hover:translate-x-1"/></div></Link>)}</div>:<Empty/>}</section></main> }
function ErrorBox(){return <div className="horizon-card border-red-200 bg-red-50 p-7 text-sm text-red-700">Unable to load the category catalog. Check the Supabase connection.</div>};function Empty(){return <div className="horizon-card p-12 text-center"><BriefcaseBusiness className="mx-auto text-slate-300" size={40}/><h2 className="mt-4 font-black">No categories available</h2><p className="mt-2 text-sm text-slate-500">No category records are currently available.</p></div>}
